# LLM API配置（支持 OpenAI SDK 格式的任意 LLM API）
# 推荐使用阿里百炼平台qwen-plus模型：https://bailian.console.aliyun.com/
# 注意消耗较大，可先进行小于40轮的模拟尝试
LLM_API_KEY=your_api_key_here
LLM_BASE_URL=https://dashscope.aliyuncs.com/compatible-mode/v1
LLM_MODEL_NAME=qwen-plus

# ===== Graphiti graph-extraction LLM (optional) =====
# The simulation LLM (LLM_MODEL_NAME) can be a reasoning model (MiniMax-M2.7,
# DeepSeek-R1), but graph extraction requires reliable structured output.
# These three vars configure a separate provider for Graphiti.  Each one
# falls back to its LLM_* counterpart when unset, so you only set the ones
# that differ from the main LLM.
# Recommended: DeepSeek-V3 (deepseek-chat) — supports json_schema, cheap.
# GRAPHITI_LLM_API_KEY=your_deepseek_or_openai_key
# GRAPHITI_LLM_BASE_URL=https://api.deepseek.com/v1
# GRAPHITI_LLM_MODEL_NAME=deepseek-chat

# ===== ZEP记忆图谱配置 =====
# 每月免费额度即可支撑简单使用：https://app.getzep.com/
ZEP_API_KEY=your_zep_api_key_here

# ===== 加速 LLM 配置（可选）=====
# 注意如果不使用加速配置，env文件中就不要出现下面的配置项
LLM_BOOST_API_KEY=your_api_key_here
LLM_BOOST_BASE_URL=your_base_url_here
LLM_BOOST_MODEL_NAME=your_model_name_here